class CommonClockListener
!!!198284.cpp!!!	onTimelineChanged(in timelineID : uint64_t) : void
    // do nothing; listener is only really used as a token so the server can
    // find out when clients die.
