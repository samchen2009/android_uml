class CstLiteralBits
!!!14728076.java!!!	fitsIn16Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (short) bits == bits;
!!!14728204.java!!!	fitsIn8Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (byte) bits == bits;
