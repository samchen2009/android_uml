class TileData
!!!9350668.java!!!	TileData(in left : int, in top : int, in right : int, in bottom : int, inout isReady : boolean, in level : int, in scale : float)
            this.left = left;
            this.right = right;
            this.top = top;
            this.bottom = bottom;
            this.isReady = isReady;
            this.level = level;
            this.scale = scale;
!!!9350796.java!!!	toString() : String
            return "Tile (" + left + "," + top + ")->("
                    + right + "," + bottom + ")"
                    + (isReady ? "ready" : "NOTready") + " at scale " + scale;
