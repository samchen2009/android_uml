class H
!!!15830284.java!!!	handleMessage(inout msg : Message) : void
                switch (msg.what) {
                    case 1:
                        onFocusGained((WindowId)msg.obj);
                        break;
                    case 2:
                        onFocusLost((WindowId)msg.obj);
                        break;
                    default:
                        super.handleMessage(msg);
                }
