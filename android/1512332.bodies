class SimpleApp
!!!9291660.java!!!	onCreate(inout icicle : Bundle) : void
        super.onCreate(icicle);

        // Create our Preview view and set it as the content of our
        // Activity
        mView = new SimpleAppView(this);
        setContentView(mView);
