class GlobalKeyReceiver
!!!13042700.java!!!	onReceive(inout context : Context, inout intent : Intent) : void
        Log.v("GlobalKeyReceiver", "Starting service.");
        // Starts service.
        context.startService(new Intent(context, GlobalKeyHandlerService.class));
