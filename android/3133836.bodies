class DeviceType
!!!13220876.java!!!	safeValueOf(in name : String) : DeviceType
            for (DeviceType deviceType : DeviceType.values()) {
                if (deviceType.name().equals(name)) {
                    return deviceType;
                }
            }
            return OTHER;
