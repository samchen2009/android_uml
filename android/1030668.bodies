class AndroidPrintStream
!!!5513740.java!!!	AndroidPrintStream(in priority : int, in tag : String)
        if (tag == null) {
            throw new NullPointerException("tag");
        }

        this.priority = priority;
        this.tag = tag;
!!!5513868.java!!!	log(in line : String) : void
        Log.println(priority, tag, line);
