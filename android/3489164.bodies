class Unsigned
!!!15248780.java!!!	compare(in ushortA : short, in ushortB : short) : int
        if (ushortA == ushortB) {
            return 0;
        }
        int a = ushortA & 0xFFFF;
        int b = ushortB & 0xFFFF;
        return a < b ? -1 : 1;
!!!15248908.java!!!	compare(in uintA : int, in uintB : int) : int
        if (uintA == uintB) {
            return 0;
        }
        long a = uintA & 0xFFFFFFFFL;
        long b = uintB & 0xFFFFFFFFL;
        return a < b ? -1 : 1;
