class SoundEvent
!!!316044.cpp!!!	set(in sample : sp<Sample>, in channelID : int, in leftVolume : float, in rightVolume : float, in priority : int, in loop : int, in rate : float) : void
    mSample = sample;
    mChannelID = channelID;
    mLeftVolume = leftVolume;
    mRightVolume = rightVolume;
    mPriority = priority;
    mLoop = loop;
    mRate =rate;
