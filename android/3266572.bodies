class TvWifiSetupActivity
!!!14154380.java!!!	getIntent() : Intent
        Intent modIntent = new Intent(super.getIntent());
        modIntent.putExtra(EXTRA_PREFS_SHOW_BUTTON_BAR, true);
        modIntent.putExtra(EXTRA_IS_FIRST_RUN, true);
        return modIntent;
!!!14154508.java!!!	onCreate(inout savedInstanceState : Bundle) : void
        super.onCreate(savedInstanceState);
