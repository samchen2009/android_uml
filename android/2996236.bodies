class GlobalKeyHandlerReceiver
!!!12214540.java!!!	onReceive(inout context : Context, inout intent : Intent) : void
        Log.v("IrGlobalKeyHandlerReceiver", "Starting service.");
        // Starts service.
        context.startService(new Intent(context, GlobalKeyHandlerService.class));
