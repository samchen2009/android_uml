class TrustStorageListener
!!!5025420.java!!!	onReceive(inout context : Context, inout intent : Intent) : void
            if (intent.getAction().equals(KeyChain.ACTION_STORAGE_CHANGED)) {
                handleCertTrustChanged();
            }
