class CharGroupInfo
!!!12880396.java!!!	CharGroupInfo(in originalAddress : int, in endAddress : int, in flags : int, in characters : int, in frequency : int, in childrenAddress : int, in shortcutTargets : ArrayList<WeightedString>, in bigrams : ArrayList<PendingAttribute>)
        mOriginalAddress = originalAddress;
        mEndAddress = endAddress;
        mFlags = flags;
        mCharacters = characters;
        mFrequency = frequency;
        mChildrenAddress = childrenAddress;
        mShortcutTargets = shortcutTargets;
        mBigrams = bigrams;
