class ProxyConsumerListener
!!!1486732.cpp!!!	onFrameAvailable() : void
    sp<BufferQueue::ConsumerListener> listener(mConsumerListener.promote());
    if (listener != NULL) {
        listener->onFrameAvailable();
    }
!!!1486860.cpp!!!	onBuffersReleased() : void
    sp<BufferQueue::ConsumerListener> listener(mConsumerListener.promote());
    if (listener != NULL) {
        listener->onBuffersReleased();
    }
