class GlobalKeyReceiver
!!!12304908.java!!!	onReceive(inout context : Context, inout intent : Intent) : void
        Log.v(TAG, "Starting service.");
        // Starts service.
        context.startService(new Intent(context, GlobalKeyService.class));
