class HistoricalCharsetNames
!!!229147.java!!!	get(inout charset : Charset) : String
        String modernName = charset.name();
        String result = historicalNames.get(modernName);
        return (result != null) ? result : modernName;
