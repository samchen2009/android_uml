class RotationActivity
!!!9147788.java!!!	onCreate(inout savedInstanceState : Bundle) : void
        super.onCreate(savedInstanceState);

        DrawingView container = new DrawingView(this);

        setContentView(container);
!!!9147916.java!!!	dipToPx(inout c : Context, in dip : int) : int
        return (int) (c.getResources().getDisplayMetrics().density * dip + 0.5f);
