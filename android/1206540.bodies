class AudioEncoderCap
!!!6795788.java!!!	AudioEncoderCap(in codec : int, in minBitRate : int, in maxBitRate : int, in minSampleRate : int, in maxSampleRate : int, in minChannels : int, in maxChannels : int)
           mCodec = codec;
           mMinBitRate = minBitRate;
           mMaxBitRate = maxBitRate;
           mMinSampleRate = minSampleRate;
           mMaxSampleRate = maxSampleRate;
           mMinChannels = minChannels;
           mMaxChannels = maxChannels;
