class ListItem
!!!13494540.java!!!	ListItem(inout res : Resources, in textResourceId : int, in imageResourceId : int, in actionTag : int)
            text = res.getString(textResourceId);
            if (imageResourceId != -1) {
                image = res.getDrawable(imageResourceId);
            } else {
                image = null;
            }
            this.actionTag = actionTag;
