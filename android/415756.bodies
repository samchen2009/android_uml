class PointerProperties
!!!989836.cpp!!!	operator ==(in other : PointerProperties) : bool
    return id == other.id
            && toolType == other.toolType;
!!!990092.cpp!!!	copyFrom(in other : PointerProperties) : void
    id = other.id;
    toolType = other.toolType;
