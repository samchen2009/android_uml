class Logger
!!!2368923.java!!!	getStream(inout name : String) : Stream
        for (int i=0; i<names.length; i++) {
            if (names[i].equals(name)) {
                return new Stream(name);
            }
        }
        return null;
