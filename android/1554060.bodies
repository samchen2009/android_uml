class MockClassHasNativeVisitor
!!!9612044.java!!!	getMethodsFound() : String
            return mMethodsFound.toArray(new String[mMethodsFound.size()]);
!!!9612172.java!!!	setHasNativeMethods(inout hasNativeMethods : boolean, in methodName : String) : void
            if (hasNativeMethods) {
                mMethodsFound.add(methodName);
            }
            super.setHasNativeMethods(hasNativeMethods, methodName);
