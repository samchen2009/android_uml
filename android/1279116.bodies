class GLES11
!!!7416588.java!!!	glPointSizePointerOES(in type : int, in stride : int, inout pointer : java.nio.Buffer) : void
        glPointSizePointerOESBounds(
            type,
            stride,
            pointer,
            pointer.remaining()
        );
        if (((type == GL_FLOAT) ||
             (type == GL_FIXED)) &&
            (stride >= 0)) {
            _pointSizePointerOES = pointer;
        }
