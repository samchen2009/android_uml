class LogPrinter
!!!4407180.java!!!	LogPrinter(in priority : int, in tag : String)
        mPriority = priority;
        mTag = tag;
        mBuffer = Log.LOG_ID_MAIN;
!!!4407308.java!!!	LogPrinter(in priority : int, in tag : String, in buffer : int)
        mPriority = priority;
        mTag = tag;
        mBuffer = buffer;
!!!4407436.java!!!	println(in x : String) : void
        Log.println_native(mBuffer, mPriority, mTag, x);
