class CstLiteralBits
!!!15144460.java!!!	fitsIn16Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (short) bits == bits;
!!!15144588.java!!!	fitsIn8Bits() : boolean
        if (! fitsInInt()) {
            return false;
        }

        int bits = getIntBits();
        return (byte) bits == bits;
