class Content
!!!2093964.java!!!	main(inout args : String) : void
        Parser parser = new Parser(args);
        Command command = parser.parseCommand();
        if (command != null) {
            command.execute();
        }
