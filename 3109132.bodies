class TunerController
!!!13045004.java!!!	TunerController(in context : DeviceControllerContext, in settings : MediaDeviceSettings, in deviceId : String)
        super(context, settings, deviceId);
        // OEM NOTE: reserve native resources here.
!!!13045132.java!!!	destroy() : void
        // OEM NOTE: release resources here.
        super.destroy();
!!!13045260.java!!!	performAction(inout event : ActionEvent) : void
        // OEM NOTE:
        // Implement tuning here.
        if (DEBUG) {
            Log.d(LOG_TAG, "Sending event " + event);
        }
!!!13045388.java!!!	tuneToChannel(inout channel : ChannelNumber) : void
        // OEM NOTE:
        // Implement tuning here.
        if (DEBUG) {
            Log.d(LOG_TAG, "Sending channel command " + channel.toString());
        }
