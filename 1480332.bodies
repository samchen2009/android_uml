class PathDestructionActivity
!!!9144076.java!!!	onCreate(inout savedInstanceState : Bundle) : void
        super.onCreate(savedInstanceState);

        MyView view = new MyView(this);
        setContentView(view);
