class Stats
!!!7584268.java!!!	Stats(inout paint : Paint)
            super(false);
            mPaint = paint;
!!!7584396.java!!!	onLoadChanged(in load1 : float, in load5 : float, in load15 : float) : void
            mLoadText = load1 + " / " + load5 + " / " + load15;
            mLoadWidth = (int)mPaint.measureText(mLoadText);
!!!7584524.java!!!	onMeasureProcessName(in name : String) : int
            return (int)mPaint.measureText(name);
