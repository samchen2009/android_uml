class BandwidthTestRunner
!!!5788428.java!!!	onCreate(inout icicle : Bundle) : void
        super.onCreate(icicle);
        String ssidStr= (String) icicle.get("ssid");
        if (ssidStr != null) {
            mSsid = ssidStr;
        }
        String serverStr = (String) icicle.get("server");
        if (serverStr != null) {
            mTestServer = serverStr;
        }
