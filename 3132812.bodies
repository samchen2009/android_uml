class Action
!!!13209612.java!!!	safeValueOf(in name : String) : Action
            for (Action action : Action.values()) {
                if (action.name().equals(name)) {
                    return action;
                }
            }
            return GENERIC;
