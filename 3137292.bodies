class LineupType
!!!13263372.java!!!	safeValueOf(in name : String) : LineupType
            for (LineupType lineupType : LineupType.values()) {
                if (lineupType.name().equals(name)) {
                    return lineupType;
                }
            }
            return OTHER;
