class Playback
!!!6346892.java!!!	isValid(in playbackStatus : int) : boolean
            boolean isValid = false;

            switch (playbackStatus) {
                case START:
                case STOP:
                case PAUSE:
                case RESUME:
                    isValid = true;
            }
            return isValid;
