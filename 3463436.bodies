class TypeIdSorter
!!!14974220.java!!!	compare(inout item1 : AnnotationItem, inout item2 : AnnotationItem) : int
            int index1 = item1.type.getIndex();
            int index2 = item2.type.getIndex();

            if (index1 < index2) {
                return -1;
            } else if (index1 > index2) {
                return 1;
            }

            return 0;
