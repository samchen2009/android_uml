class ProviderRefCount
!!!2560396.java!!!	ProviderRefCount(inout inHolder : ContentProviderHolder, inout inClient : ProviderClientRecord, in sCount : int, in uCount : int)
            holder = inHolder;
            client = inClient;
            stableCount = sCount;
            unstableCount = uCount;
