class GLES30
!!!15881740.java!!!	glVertexAttribIPointer(in index : int, in size : int, in type : int, in stride : int, inout pointer : Buffer) : void
        glVertexAttribIPointerBounds(
            index,
            size,
            type,
            stride,
            pointer,
            pointer.remaining()
        );
