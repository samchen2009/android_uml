class LightingColorFilter
!!!6376844.java!!!	LightingColorFilter(in mul : int, in add : int)
        native_instance = native_CreateLightingFilter(mul, add);
        nativeColorFilter = nCreateLightingFilter(native_instance, mul, add);
