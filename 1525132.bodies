class VelocitySelectedListener
!!!9345932.java!!!	onItemSelected(inout parent : AdapterView, inout view : View, in position : int, in id : long) : void
            String speedStr = parent.getItemAtPosition(position).toString();
            int speedInt = Integer.parseInt(speedStr);
            mWeb.setAutoScrollSpeed(speedInt);
